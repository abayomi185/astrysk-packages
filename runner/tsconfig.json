{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "composite": true,
    "strict": true,
    "module": "esnext",
    "baseUrl": "." /* Base directory to resolve non-absolute module names. */,
    "paths": {
      "@*": ["src/*", "./*"],
      "@src/*": ["src/*"]
    }
  }
}
